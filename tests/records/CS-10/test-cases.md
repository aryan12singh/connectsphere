# CS-10 — Test Case Specification (IS212 / IEEE 829)

> Blue section — written once. Yellow execution results are in `test-runs/<YYYY-MM-DDTHH-mm-ss>.md` per run (generated by `tests/scripts/compile-test-run.ts`).

Story: *As a ConnectSphere user, I want to log in securely and reach the correct starting screen so that I can access the functions available to my role.*
Roles per AC (north star): `EVENT_ORGANISER | EVENT_COORDINATOR | VENUE_STAFF | ATTENDEE | TECHNICAL_SUPPORT_STAFF` — no temporary enum details.

| Test Case ID | Test Scenario | Pre-conditions | Test Steps | Test Data | Expected Result | Date of Creation |
|---|---|---|---|---|---|---|
| TC-CS10-01 | Valid login establishes authenticated session and returns configured role | Seeded users (one per AC role); browser at `/login`; no session cookie | 1. Navigate to `/login` 2. Enter valid `email`+`password` 3. Click Sign in | `email=organiser@example.com`, `password=Password123!` (+ 4 other roles) | BFF `POST /api/auth 200 {user.role}` (one of the 5 AC roles, no token in body), sealed `nuxt-session` cookie set (`HttpOnly`, `SameSite=Lax`) | 2026-05-14 23:30:00 |
| TC-CS10-02 | Invalid credentials — generic error, no session | Same seeded users; at `/login` | 1. Navigate to `/login` 2. Enter invalid password 3. Click Sign in | `email=organiser@example.com`, `password=wrong` | `401 Invalid credentials` — same text for bad email vs bad password, no cookie, stays on `/login`, `role=alert` shown | 2026-05-14 23:30:00 |
| TC-CS10-03 | Unauthenticated request for protected info denied server-side | No sealed session | 1. Direct `GET /api/events` (or any protected `GET /api/**`) without a session | `Cookie: (none)` | `401 Unauthorized` from BFF via `requireUserSession` | 2026-05-14 23:30:00 |
| TC-CS10-04 | Logout and session expiry prevent further protected access (incl. direct endpoint) | Logged in then either `DELETE /api/auth` or cookie expired | 1. Login 2. `DELETE /api/auth` (or wait expiry) 3. Protected `GET` | Session from TC-CS10-01 then cleared/expired | `DELETE /api/auth 200 {ok:true}`, subsequent protected `GET` → `401`; UI back to `/login` | 2026-05-14 23:30:00 |
| TC-CS10-05 | Account without recognised authorisation denied, not defaulted to privileged role | DB has user with `role=NULL`/`GHOST` | 1. Login with ghost-role user | `email=ghost@example.com` with no valid AC role | `403` (or `401` at login) — never assigned `EVENT_ORGANISER`/`TECHNICAL_SUPPORT_STAFF` default; UI shows denied | 2026-05-14 23:30:00 |
| TC-CS10-06 | Successful login opens role-appropriate starting screen; data respects ownership/assignment | Same seeded users as TC-CS10-01 | 1. Login per role 2. Observe starting screen | One login per each of the 5 AC roles | Single homepage `/` renders role-appropriate content; e.g. organiser sees own events, coordinator sees review queue, etc. Data filtered server-side by ownership/assignment | 2026-05-14 23:30:00 |
| TC-CS10-07 | Unauthenticated interface access redirects to login | No sealed session; browser requests a protected route | 1. Navigate directly to `/` without a session 2. Middleware checks `useUserSession().loggedIn` / refreshes via built-in session endpoint | `Cookie: (none)` | Middleware finds no authenticated session and redirects to `/login` | 2026-09-18 23:14:01 |
| TC-CS10-08 | Avatar popup shows profile and signs out; error text when unreachable | Authenticated (or session failed) on any navbar page | 1. Click profile avatar 2. Observe popup 3. Click Sign out (or break the session) | Session user `Organiser One / organiser@example.com / EVENT_ORGANISER` | Popup shows name, email, role and Sign out; sign-out clears session and returns to `/login`; unreachable profile shows an error text in the popup | 2026-09-20 10:00:00 |

*RBAC note for TC-CS10-06: Homepage is a single `/` with server-enforced RBAC. No per-role redirect paths are part of the AC.*

*Contract change 2026-09-20 10:00:00: BFF auth is now RESTful `POST/DELETE /api/auth` on sealed `nuxt-session` cookies (nuxt-auth-utils); the opaque token never appears in any body; `ADMIN` removed — backend stores `TECHNICAL_SUPPORT_STAFF` directly; custom `GET /api/auth/session` deleted in favour of the built-in session + `requireUserSession` on protected routes. TC-01/03/04/07 rows updated to match; automated spec (`tests/specs/CS-10.spec.ts`) updated in the same change.*

*Navbar refactor 2026-09-20 10:00:00: header extracted byte-identical into `app/layouts/default.vue` (route-aware `aria-current` preserves per-page state; `login.vue` opts out via `layout: false`); static avatars replaced by `UserMenu` (shadcn Popover + Avatar/Badge/Separator/Button) everywhere the layout renders. TC-08 added for the popup contract.*

*Refresh-401 fix 2026-09-20: dashboard fetched events with plain `$fetch`, which drops browser cookies when `useAsyncData` runs on the server (hard refresh) — `requireUserSession` answered 401. Final shape (B): page loads via `useFetch('/api/events', { key: 'organiser-events' })`, which resolves relative URLs through the request-scoped fetcher automatically (verified in framework source). Interim per-call `useRequestFetch` wiring and the shared `BffFetcher`/`callBff` helper were removed again; lib clients are plain injectable `$fetch` wrappers. TC-03 keeps the unauthenticated-401 plus an authenticated (real iron-sealed cookie) 200 case. Note: `cookie` is a forbidden header — undici strips it from `new Request()` — so cookie-bearing unit tests invoke the real handler with a real `H3Event` instead.*

*No-lib direction 2026-09-20: `app/lib/auth.ts` + `app/lib/events.ts` deleted (`cn` in `lib/utils.ts` stays — design-system, not BFF). Pages call `useFetch('/api/...')` directly; event contract types live in `server/api/events.get.ts` (api layer — mocks import from there); `index.vue` keeps a local payload guard for its malformed branch. TC-01/02/06 re-automated at login-page level (mocked `useFetch`); TC-04 client test removed (logout covered by TC-08 UI + DELETE handler tests); TC-05 re-automated STRONGER at the BFF boundary (ghost + missing roles → 403, no session) with a 5-role allowlist hardened into `auth.post.ts` after an observed RED (200 vs 403).*
